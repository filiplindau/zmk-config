# Expect 2 peripherals (Left and Right)
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

# Keep the dongle awake to maintain the PC connection
CONFIG_ZMK_SLEEP=n

# Force a custom Bluetooth name
CONFIG_ZMK_KEYBOARD_NAME="Sweep Dongle"